class Place {
  public id: number;
  public title: string;
  public image: string;
  constructor(id: number, title: string, image: string) {
    this.id = id;
    this.title = title
    this.image = image
  }
}

export default Place